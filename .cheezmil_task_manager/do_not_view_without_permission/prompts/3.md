


```plaintext
我给 CTI 的参数其实是“正常的”，但问题出在：工具层没把调
  用结果返回给我（60s 等不到 tools/call 响应），所以我确 
  实“没有拿到 CTI 信息”，不是我没传参数。

  我当时发的（核心）参数长这样（不同终端 id 试了几次）： 

  - terminalId: 例如 fe-build-run2
  - cwd: D:\\CodeRelated\\xdx_toolbox
  - input: node start_build_fe_xdx_toolbox.mjs; ct start 
    -- node start_fe_xdx_toolbox.mjs
  - mode: this_command_output
  - waitForOutput: 1
  - wait: { mode: 'pattern' 或 'idle', timeoutMs: 600000,
    includeIntermediateOutput: true, patternRegex: true, 
    pattern: '(error|...|vite v|...)' }

  正常情况下，CTI 会返回：

  - terminalCreated
  - status.isRunning/hasPrompt
  - delta.text（实时输出）
  - lastCommand/pendingCommand

  但你看到的那次报错是工具层面的：

  - timed out awaiting tools/call after 60s

  这意味着：请求已经发给 CTI 了，但“框架没收到 CTI 的回  
  包/回包没在时限内到达”，所以我拿不到任何 status/delta  
  去判断“到底有没有在跑”，我才会误判然后重试，导致重复启 
  动。

---
这是错误的
踏马的禁止任何超时时间，去掉歧义字段，改成最大等待时间，注意功能不要重复，必须明确提示客户端必须渐进式设置最大等待时间，同时在底层禁止接收一开始就直接设置最大等待时间为1分钟以上的时间，若该终端超过8次设置了1分钟以内的最大等待时间，则提示给客户端可设置1分钟以上的最大等待时间，注意等待时间结束必须按照模式返回消息，而不是返回报错。
```


```plaintext
# 剔除掉<span data-v-87c47fa7="" class="luxury-terminal-shell" title="pwsh.exe">pwsh.exe</span>，没必要这个元素。

# 现在终结终端会报错
DELETEapi-service. ts:172
http://1ocalhost:1106/api/terminals/ws1-test-terminal1 400 (Bad Request)
Error deleting terminal: Error: Failed to terminateHomeView. vue : 203
terminal: {"statusCode":40o,"code":"FST_ERR_CTP_EMPTY_JSON_BODY","error":"Bad
Request","message":"Body cannot be empty when content-type is set to
application/json'
at D (HomeView. vue:185:13)
修正

# 在tab这里新增一个复制图标把<span data-v-87c47fa7="" class="luxury-terminal-id">wsl-test-terminal</span>里面显示的终端名称点击图标后复制。

# 终端渲染有些问题，旧的输出会覆盖住命令行所处的位置，我也不知道什么情况，也不知道咋具体描述，反正就是乱套了，有些莫名其妙的覆盖住了，但是命令行的光标还在上面，下面却出现了之前乱七八糟的输出。

# 设置页面点击保存设置后，页面所处的高度位置不要变化，要不然每次都要滚动下来，太麻烦了。

# 黑名单命令现在还是失效的，我明明在设置添加了Write-Host，结果MCP服务器还是可以接收并成功执行，你耍我呢？？修正。

# 有些命令是交互式的，必须判断当前终端是不是进入了交互式的状态，若进入了交互式的状态，则必须返回给MCP客户端一条信息“该终端进入了交互状态，请交互或新开终端，否则新的输入命令无法执行”。
```

```plaintext
- 在顶部导航栏新增一个组件，点击后终结所有终端，注意只读模是要可以终结终端的。并在顶部导航栏这里新增一个搜索框，可以快速定位到某个tab。顶部导航栏的这个”38活跃00非活跃0已终止“这些没啥用剔除掉。在顶部导航栏添加项目版本号，版本号遵循本地的VERSION这个文件，同时每次启动后端都检查是否和https://raw.githubusercontent.com/cheezmil/cheezmil-terminal-interactive/refs/heads/main/VERSION，仓库最新版本一致，若有新版本则在版本号旁边写出有新版本。

- 把重连和清空这个图标剔除掉，没啥用。

- 在tabs那里弄置顶图标，同时也要显示用了什么shell。“active”文字去掉不需要。


- 在终端选中文本后，按右键要能直接复制啊！

- document.querySelector("#app > div > div > main > div > div > main")这个容器有多余的上下padding，去掉。


- 修正config.yml.example ，把config.yml新的配置添加到example中。
```
```plaintext
有报错了，你用chrome mcp查看前端报错，然后修正，具体表现就是我点击侧边栏的每一个tab，右边对应的document.querySelector("#terminal-mcphub-main")都不显示任何的内容，修正

- 工具被调用后，前端不会自动获取到新建的终端显示，非要我手动刷新页面才显示tab，这是错误的，修正。

```