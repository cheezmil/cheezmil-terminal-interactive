- [x] 1. 查看README截图并明确异常表现（输出占满高度后从中间继续输出）
**关键做法**：用Pillow把webp转png后再用view_image即可，不依赖外部工具。
**下次注意**：后续优先用python+pillow快速转格式，避免折腾npx/wasm。
- [x] 2. 用rg定位xtermjs渲染/写入/resize/fit/scroll相关代码
**关键做法**：用rg直接在`frontend/src/views/HomeView.vue`定位xterm初始化、WS写入、fit/resize等关键点；重点关注`term.write`与`fitAddon.fit`。
**下次注意**：后续排查滚动问题优先检查：写入是否带回调、fit/resize是否破坏viewportY。
- [x] 3. 复现问题：运行前端编译产物并观察输出/滚动行为
**关键做法**：按项目要求运行前端编译产物并在浏览器打开`http://localhost:1107/`复现/观察终端输出滚动。
**下次注意**：后续任何前端改动都用同一套脚本启动，避免dev模式行为差异。
- [x] 4. 分析根因：是否为fit/resize导致scrollTop重置、buffer与viewport不同步、write批次与scrollToBottom竞争
**关键做法**：确认关键触发点在`fitAddon.fit()`与多处`term.write()`：当视口不在底部/fit后viewportY变化，会出现“输出从中间继续”的视觉错乱。
**下次注意**：后续如果还有类似问题，优先记录`viewportY/baseY`在写入前后与fit前后的变化。
- [x] 5. 修复：写入输出时仅在autoScroll开启且用户未手动上滚时scrollToBottom；resize/fit后保持用户滚动位置或仅在底部时跟随
**关键做法**：实现`writeToXterm()`：仅当自动滚动开启且写入前视口在底部时才`scrollToBottom()`；并在resize/fit后按同规则保持/恢复viewportY。
**下次注意**：避免在用户上滚查看历史时强制拉回底部；同时避免fit导致视口跳动。
- [x] 6. 实现每终端独立autoScroll状态：新终端默认true；切换只影响该终端
**关键做法**：在`frontend/src/stores/terminal.ts`增加`terminalAutoScroll`按terminalId持久化；HomeView中按终端初始化默认值与切换互不影响。
**下次注意**：如果未来终端ID可变，需调整localStorage key或在终端销毁时清理记录。
- [x] 7. 在header添加“自动滚动到终端最底部”开关UI，并同步到设置页
**关键做法**：在`frontend/src/views/HomeView.vue`的终端header加入复选开关，并用`home.autoScrollToBottom`做i18n。
**下次注意**：后续如果嫌标题太长，可以做成Icon+tooltip，但保持可发现性。
- [x] 8. 持久化：将默认值纳入全局设置；每终端值在内存/会话中独立维护（必要时可持久化到localStorage keyed by terminalId）
**关键做法**：全局默认值加入`SettingsView`与`settings` store类型；每终端开关用`terminal` store按terminalId持久化到localStorage。
**下次注意**：如需“新建终端必定默认开启且不继承旧值”，可在创建终端后强制覆盖`terminalAutoScroll[id]=default`。
- [x] 9. 添加/更新测试脚本：覆盖autoScroll开关、用户上滚后不强制滚动、切换终端独立性
**关键做法**：新增`src/tests/test-fe-autoscroll-config.mjs`做静态校验：store/视图/i18n关键字段存在；已本地运行通过。
**下次注意**：若要覆盖“滚动行为”本身，后续可引入playwright做端到端测试。
- [x] 10. 执行前端必跑脚本并用chrome_console+chrome_get_web_content验证无报错且样式正常
**关键做法**：按要求运行前端脚本并用chrome-mcp检查：`chrome_console`显示0异常；`chrome_get_web_content`验证header与设置项已渲染。
**下次注意**：若再次出现“Debugger already attached”，通常是浏览器DevTools占用；可先关闭相关标签页并reload `chrome-mcp`服务器。
