- [x] 1. 定位产生“[省略前面 N 行]”的代码路径与触发条件
**关键做法**：用 `rg` 直接定位到 `src/terminal-manager.ts` 中插入的“... [省略前面/后续 N 行] ...”标记逻辑（来自 smart/head/tail/head-tail 模式）。
**下次注意**：后续要确保前端请求 `mode=full` 且支持 `maxLines=0` 才能拿到完整缓冲历史。
- [x] 2. 设计并实现终端输出的增量/分页协议（游标、批量大小、最大缓存）
**关键做法**：在 `src/output-buffer.ts` 的 `read()` 新增 `direction`（forward/backward），配合 `since` + `maxLines` 实现可分页且不丢行的读取；同时后端路由解析允许 `0` 值传参。
**下次注意**：默认保持 backward（取最新）避免影响旧调用；前端历史加载显式用 forward 分页。
- [x] 3. 调整前端 xterm 渲染：只追加新增内容，避免全量重绘
**关键做法**：`frontend/src/views/HomeView.vue` 历史加载改为 `mode=full + direction=forward` 分页读取并分块写入 xterm；`frontend/src/stores/terminal.ts` 对历史缓存同样做 200k 字符上限，避免 localStorage 过大。
**下次注意**：如果后端缓冲上限需要更大/更小，可再调 `OutputBuffer` 的 `maxSize` 与前端分页 `PAGE_LINES/MAX_PAGES`。
- [x] 4. 补齐相关 i18n 文案/类型定义，确保跨平台兼容
**关键做法**：同步更新 `src/types.ts` 中的 `BufferReadOptions`/`TerminalReadOptions`/`ReadTerminalInput` 类型，新增 `direction` 字段，保持前后端/SDK 类型一致。
**下次注意**：若未来要把 `direction` 暴露到 MCP 工具层（read_terminal），需要一并更新 `src/mcp-server.ts` 的参数校验与文档输出。
- [x] 5. 按项目脚本构建并启动前后端，chrome-console 验证无报错
**关键做法**：按要求执行了前端 `node start_build_fe_cheezmil-terminal-interactive.mjs; ct start -- node start_fe_cheezmil-terminal-interactive.mjs`，后端 `node start_build_be_cheezmil-terminal-interactive.mjs` + 启动/重载/测试脚本；并用 `chrome_console` 确认页面无异常报错。
**下次注意**：如果需要进一步确认 UI 内不再出现省略标记，可在终端输出很多行后刷新页面，观察历史输出开头不会再出现“省略前面/后续 N 行”。
