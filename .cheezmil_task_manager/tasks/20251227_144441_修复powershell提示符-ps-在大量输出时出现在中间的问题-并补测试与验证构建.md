- [x] 1. 定位前端xterm/后端pty输出拼接与提示符检测代码
**关键做法**：用rg直接定位HomeView/TerminalDetailView里历史分块写入与WS输出写入点，确认可能存在交错风险。
**下次注意**：后续改动要保证所有写入都走同一串行队列，避免任何异步分块写入时被实时输出穿插。
- [x] 2. 复现问题并确认触发条件(无尾换行/CR/分片乱序)
**关键做法**：用“历史分块写入 + 实时输出同时到达”的并发模型解释提示符插入中间，并用串行写入队列作为根因修复方向。
**下次注意**：如果未来还有“刷新/重连/重新加载历史”的入口，必须也走同一写入队列，避免再出现交错。
- [x] 3. 实现修复：规范化CR/LF、按行缓冲、提示符只在行首且在flush后渲染
**关键做法**：在HomeView/TerminalDetailView对所有term.write写入统一走串行写入队列；历史加载也在队列里执行，避免与WS输出交错。
**下次注意**：若后续要做更精细的提示符检测/CRLF规范化，可在队列内再做行缓冲处理，但不要在队列外直接写term。
- [x] 4. 新增/更新测试脚本覆盖该场景(禁止黑名单命令)
**关键做法**：新增`src/tests/test-serial-write-queue.mjs`覆盖“history分块写入时prompt到达”的顺序保证；测试不包含任何黑名单命令。
**下次注意**：如需进一步覆盖“TerminalDetailView先WS后history”的回归，可再加一例模拟先enqueue live再enqueue history 的策略选择。
- [x] 5. 按规定脚本构建FE/BE并用chrome_console+页面内容检查
**关键做法**：按规定脚本重新build并启动前端产物，并通过chrome_console确认无异常。
**下次注意**：如果仍能复现提示符插入中间，下一步需要抓取WS输出到达时序并确认是否还有其他路径绕过writeQueue。
