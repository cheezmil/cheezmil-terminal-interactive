- [x] 1. 确认 `VERSION` 文件内容与前端显示来源
**关键做法**：用 `rg --context` 直接定位到 `frontend/vite.config.ts` 的构建时注入版本与 `src/version-service.ts` 的运行时读取/远端检查逻辑，确认前后端版本来源差异。
**下次注意**：如果前端显示版本不对，优先检查是否使用了构建时注入常量或浏览器缓存的旧产物。
- [x] 2. 在代码中定位版本字符串/更新检查逻辑（rg 带上下文）
**关键做法**：定位到 `frontend/src/App.vue` 的 `loadVersionInfo()`：原先强制使用 `__CTI_VERSION__` 作为 currentVersion，导致前端产物旧时显示落后。
**下次注意**：`/api/version` 已返回 `currentVersion`，前端应优先使用它以保证与本地 `VERSION` 一致。
- [x] 3. 修复：前端显示版本与更新检查都读取同一来源（VERSION）
**关键做法**：修改 `frontend/src/App.vue`：`currentVersion` 优先使用后端 `/api/version` 返回的 `data.currentVersion`（运行时读取本地 `VERSION`），再 fallback 到构建时注入的 `__CTI_VERSION__`。
**下次注意**：这样即使前端 dist 旧，版本显示也不会落后；版本比较也基于同一 currentVersion。
- [x] 4. UI：将“有新版本： vX.Y.Z”改为上箭头图标（保留可访问性/tooltip）
**关键做法**：修改 `frontend/src/App.vue` 顶部版本区域：用 `SvgIcon name="arrowUp"` 替代“有新版本： vX.Y.Z”文本，并用 `title`/`aria-label`/`sr-only` 保留可访问性信息。
**下次注意**：图标映射在 `frontend/src/components/ui/svg-icon.vue` 新增 `arrowUp`（Lucide）。
- [x] 5. 按项目规则构建前端产物并启动，Chrome 控制台/页面内容验证
**关键做法**：按规则执行 `node start_build_fe_cheezmil-terminal-interactive.mjs; ct start -- node start_fe_cheezmil-terminal-interactive.mjs`，并用 Chrome 刷新后通过 `chrome_console` 确认无异常。
**下次注意**：若 `chrome_console` 报 Debugger 已附加，可关闭 DevTools 或重启 RooCode MCP；本次通过 `restart_roocode_mcp.ps1` 恢复连接。
