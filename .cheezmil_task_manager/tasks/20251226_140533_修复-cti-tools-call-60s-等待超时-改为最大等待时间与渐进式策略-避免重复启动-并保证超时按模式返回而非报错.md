- [x] 1. 定位导致 'timed out awaiting tools/call after 60s' 的调用链与硬编码超时点
**关键做法**：用 `rg -n -C` 直接在 `src/mcp-server.ts` 找到 `MAX_SINGLE_CALL_WAIT_MS = 50_000` 与 `waitTimeoutMs` 计算点（1278行附近）。
**下次注意**：问题根因之一是 schema 只声明了 `waitMs`，但实现读取的是 `wait.timeoutMs`，导致客户端传 `timeoutMs` 会被 zod strip 掉，从而出现“没等到输出/判断错误”。
- [x] 2. 梳理 CTI interact_with_terminal 的 wait / waitForOutput / mode 行为与现有返回结构（terminalCreated/status/delta/lastCommand）
**关键做法**：已梳理：写入前取 cursor，`mode=this_command_output` 用 baselineSince=cursor-before-write；wait 轮询 readFromTerminal，最终 structuredContent 写入 `wait.reason`/`delta`/`status` 等。
**下次注意**：需要把 `waitForOutput`/`waitMs`/`timeoutMs` 统一成单一 `maxWaitMs`，并在 timeout 时返回结构化结果而不是成功文案或抛错。
- [x] 3. 定义新参数：去歧义字段（统一为 maxWaitMs/await 模式），并保持向后兼容或提供迁移路径
**关键做法**：统一等待参数为 `wait.maxWaitMs`，并保留 `wait.timeoutMs`/`wait.waitMs`/`waitForOutput` 作为兼容输入；schema 使用 `.passthrough()` 避免工具层字段被 strip。
**下次注意**：后续要在返回结构里只强调 `maxWaitMs`，并给出渐进式建议；同时修复单次等待硬上限未生效的问题。
- [x] 4. 实现：等待截止时返回结构化结果（如 kind='wait_timeout'）而非抛错，且按 mode 产出相同 shape
**关键做法**：`src/mcp-server.ts` 在 wait 到期时设置 `structuredContent.kind='wait_timeout'` 且 `wait.reason='timeout'`，并返回文本提示客户端继续渐进式轮询；不再抛错。
**下次注意**：后续如果发现客户端依赖旧字段 `timeoutMs`，需要在客户端侧迁移，而不是在服务端继续输出歧义字段。
- [x] 5. 实现：服务端渐进式最大等待时间策略（初次禁止>60s；同 terminalId 8 次<=60s 后允许>60s；给出明确提示信息）
**关键做法**：实现 `progressiveWaitTracker`：同 terminalId 在累计 8 次 `<=60s` 的 `maxWaitMs` 尝试之前拒绝 `>60s` 请求，返回 `kind='progressive_wait_required'` 的非错误结果，并明确提示渐进式设置。
**下次注意**：目前计数在 kill/clean 事件里清理；如需更强健可加 TTL 清理（暂未加，避免引入额外复杂度）。
- [x] 6. 实现：对过大 maxWaitMs 的拒绝响应不视为错误（返回可行动的提示）
**关键做法**：对过大 maxWaitMs 的处理改为结构化提示返回（`kind='progressive_wait_required'`），并保证不会执行写入以避免重复启动。
**下次注意**：如果你还想加“同终端短等待累计 8 次后给出明确提示可设置 >60s”的额外文案，我可以加到 structuredContent.progressiveWait 里。
- [x] 7. 新增/修改测试脚本：覆盖 wait_timeout 返回、渐进式限制、8 次后放行、以及不同 wait mode 行为
**关键做法**：已更新 `src/tests/test-mcp-client.mjs`：新增 wait_timeout 与 progressive 策略测试，并把旧用法迁移到 `wait.maxWaitMs`。
**下次注意**：下一步按项目规定跑后端 build + 启动 + 集成测试脚本（你刚才中断了）。
- [x] 8. 更新 README/协议文档：指导客户端渐进式设置 maxWaitMs，并解释服务端限制与返回码/字段
**关键做法**：按你的要求：未更改 `README.md` / `README-ZH.md`，不进行文档更新；相关调用说明改由代码 schema/structuredContent 提示承担。
**下次注意**：如果你允许，我可以改为新增独立文档文件（不动 README）来写协议说明。
- [ ] 9. 运行后端构建与集成测试脚本验证（按项目规定脚本）
