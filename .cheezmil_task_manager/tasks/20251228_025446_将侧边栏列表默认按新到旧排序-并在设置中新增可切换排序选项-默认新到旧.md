- [x] 1. 定位 /html/body/div/div/div/main/div/div/aside/div[2] 对应的前端组件与数据来源
**关键做法**：用 `rg -n ... -C` 直接在 `frontend/src/views/HomeView.vue` 定位到 `displayedTerminals` 计算属性与侧边栏 `v-for`。
**下次注意**：后续需要同步调整“默认选中终端”的逻辑，避免仍然按未排序的 `terminals[0]` 选中。
- [x] 2. 实现列表排序逻辑（新到旧/旧到新）并确保默认新到旧
**关键做法**：在 `displayedTerminals` 中：先按置顶，再按 `created` 时间（由设置决定新旧），最后用 `id` 做稳定回退；同时把默认选中逻辑改为使用 `displayedTerminals[0]`。
**下次注意**：下一步在 `SettingsView.vue` + i18n + config.yml 增加 `terminal.terminalListSortOrder`，并确保默认值为 newest。
- [x] 3. 在设置（pinia/i18n/UI）中新增排序选项并持久化
**关键做法**：在 `frontend/src/views/SettingsView.vue` 增加 `terminal.terminalListSortOrder`（默认 newest）并用 `RadioGroup` 提供切换；同时补齐 `frontend/src/i18n/locales/en.json`/`zh.json`，并在 `config.yml` 与 `config.example.yml` 同步新增键。
**下次注意**：下一步补测试脚本（静态校验）并跑前端 build+start，然后用 chrome-console 看报错。
- [x] 4. 补充/更新对应的测试脚本以覆盖排序切换与默认行为
**关键做法**：新增 `src/tests/test-fe-terminal-list-sort-order.mjs` 做静态校验：HomeView/SettingsView/i18n/config.yml/config.example.yml 必须包含排序相关字段。
**下次注意**：接下来按项目规定跑前端 build+start；然后用 chrome-mcp refresh + console 检查报错与样式。
- [x] 5. 构建前端产物并启动前端，Chrome 检查 console/样式
**关键做法**：按项目规定执行 `node start_build_fe_cheezmil-terminal-interactive.mjs; ct start -- node start_fe_cheezmil-terminal-interactive.mjs`，并用 `chrome_console` 确认无异常。
**下次注意**：最后只需确认 `config.yml` 与 `config.example.yml` 已同步新增 `terminalListSortOrder`（已做），即可勾选任务6。
- [x] 6. 同步检查 config.yml / config.example.yml（如有新增配置）
**关键做法**：已在 `config.yml` 与 `config.example.yml` 同步新增 `terminal.terminalListSortOrder: 'newest'`，确保示例配置与实际配置一致。
**下次注意**：无
