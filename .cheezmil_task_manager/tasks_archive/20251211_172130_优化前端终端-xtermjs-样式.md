- [x] 1. 调研 1Panel 的 xtermjs 使用方式
- [x] 2. 分析本项目现有 xtermjs 集成与样式
- [x] 3. 基于调研结果优化 xtermjs 样式并验证效果
- [x] 4. 微调 xterm 字符间距并尽量对齐 1Panel 终端视觉效果
**关键解决方法**：统一了所有 xterm 实例的字体配置（Monaco/Menlo/Consolas/Courier New），同时去掉了全局和局部 CSS 中对字体和行高的强制覆盖，只通过 xterm.js 的配置项控制字体度量，并将 letterSpacing 设置为 -1 以略微收紧字符间距，避免看起来每个字母之间像多了一个空格。前端重新构建并通过浏览器控制台检查无报错。
- [x] 5. 尝试将终端字体切换为微软雅黑并观察字符间距效果
**关键解决方法**：将 HomeView、TerminalDetailView、TerminalTestView 三个视图中 xterm 的 fontFamily 都改为优先使用 "Microsoft YaHei", 微软雅黑，其后再回落到 Monaco/Menlo/Consolas/Courier New 等等宽字体，同时把 letterSpacing 恢复为 0，这样可以纯粹观察字体本身对字符间距的影响。前端已重新构建并刷新页面，可直接对比使用微软雅黑后的终端字距观感。
- [x] 6. 解释 API 初始化失败原因并调整错误提示为顶部 toast
**关键解决方法**：在 HomeView 的 API 初始化失败分支中，将错误 toast 文案改为中英文双语，清楚说明“无法初始化后端 API 服务，请确认 1106 端口的后端已启动”；同时在 HomeView 和 SettingsView 中的 <Toaster /> 组件上设置 position="top-center"，并在包装组件 Sonner.vue 中通过 fixed 定位确保 toast 作为浮层显示在页面顶部，而不是参与文档流。前端已重新构建并刷新页面，浏览器控制台仍会记录连接 1106 失败的错误，但用户可通过顶部弹出的 toast 清晰获知原因。
- [x] 7. 继续使用CTI打印一些文字
- [x] 8. 按 DejaVu Sans Mono + 中文双倍宽字体方案重新设置前端 xterm 字体
**关键解决方法**：参考论坛方案“XTerm.vt100.faceName: DejaVu Sans Mono / faceNameDoublesize: WenQuanYi Zen Hei Mono”，将前端三个 xterm 实例（HomeView、TerminalDetailView、TerminalTestView）的 fontFamily 统一为：CTIDejaVuSansMono, "DejaVu Sans Mono", "WenQuanYi Zen Hei Mono", "WenQuanYi Zen Hei", "Microsoft YaHei Mono", "JetBrains Mono", Monaco, Menlo, Consolas, "Courier New", monospace，并把 lineHeight 调整为 1.1、letterSpacing 固定为 0，让 ASCII 字符优先使用 DejaVu Sans Mono，中文则回退到等宽中文字体，从根源上缓解“字距过大像多一个空格”的问题。前端已重新构建并刷新页面，可在终端中对比英文与中文混排的间距表现。
- [x] 9. 引入 WenQuanYi Zen Hei Mono 字体文件并在前端统一使用
**关键解决方法**：使用 Invoke-WebRequest 从 GitHub 仓库 vuleetu/.fonts 下载 wqy-zenhei.ttc 到 frontend/public/fonts/WenQuanYiZenHeiMono.ttc，然后在 frontend/src/style.css 中新增 @font-face 定义 CTIWenQuanYiZenHeiMono，并移除不再使用的 JetBrains Mono Web 字体（删除 CTIJetBrainsMono 的 @font-face 声明和 JetBrainsMono-Regular.woff2 文件）。同时将 HomeView、TerminalDetailView、TerminalTestView 三处的 xterm fontFamily 统一为：CTIDejaVuSansMono, "DejaVu Sans Mono", CTIWenQuanYiZenHeiMono, "WenQuanYi Zen Hei Mono", "WenQuanYi Zen Hei", "Microsoft YaHei Mono", "JetBrains Mono", Monaco, Menlo, Consolas, "Courier New", monospace；前端已重新构建并重启，浏览器控制台无报错，终端现在优先使用项目内置的 DejaVu Sans Mono + 文泉驿正黑等宽组合。
- [x] 10. 清理内置终端字体，改回 xterm 默认字体配置
**关键解决方法**：删除了 frontend/public/fonts 目录下自带的 DejaVuSansMono.ttf 与 WenQuanYiZenHeiMono.ttc，并在 frontend/src/style.css 中移除了对应的 @font-face 定义，仅保留一条说明“终端字体改为使用 xterm.js 默认等宽字体栈”。同时将 HomeView、TerminalDetailView、TerminalTestView 三处 new Terminal(...) 的配置中移除了所有 fontFamily、lineHeight、letterSpacing 的自定义，只保留 fontSize（12）、theme 等基础配置，从而完全回退到 xterm.js 自带的默认 monospace 字体与字距行为。前端已重新构建并重启，浏览器控制台无错，你现在看到的终端字体就是纯 xterm 默认栈的效果。
