- [x] 1. 定位 terminal-manager/mcp-server 的输入与换行逻辑
**关键做法**：使用 rg 定位到 src/terminal-manager.ts 的 writeToTerminal/normalizeNewlines，以及 src/mcp-server.ts 的 interact_with_terminal 处理逻辑。
**下次注意**：无
- [x] 2. 实现交互式模式下多行输入默认追加回车的修复
**关键做法**：为“类对话交互程序”多行输入默认追加回车提交：writeToTerminal 在检测到 Claude Code 标记时对 multiline 默认 autoAppend=true。
**下次注意**：避免对 alternateScreen(vim) 默认追加回车造成状态错乱。
- [x] 3. 扩展 interact_with_terminal：enter/esc/double_esc 等特殊键
**关键做法**：扩展 specialOperation 支持 enter/esc/double_esc，并保持原 ctrl_c/ctrl_z/ctrl_d 兼容。
**下次注意**：注意兼容 ctrl_c 与 ctrl+c 这种写法。
- [x] 4. 新增 keys/keySequence 支持（可一次性发送多个按键+间隔）
**关键做法**：新增 keys/keySequence/keyDelayMs：支持一次性发送多个按键/文本并可配置间隔时间，按顺序逐个写入 PTY。
**下次注意**：keys/keySequence 模式强制 appendNewline=false，避免额外换行干扰交互。
- [x] 5. 按项目脚本构建并运行后端自测用例
**关键做法**：按规定脚本执行构建、启动、reload，并用 src/tests/test-mcp-client.mjs + 额外 node 冒烟验证 keySequence+enter 可执行命令。
**下次注意**：无
- [x] 6. 增强 OutputBuffer：过滤/处理控制字符（退格等）并提升交互式可读性
**关键做法**：在 src/output-buffer.ts 的 append/handleEscapeSequence 中补齐控制字符处理（退格/DEL 删除、Bell/FormFeed 忽略、其余C0控制字符丢弃）并对 ESC[K/J 做最小语义清空当前行（仅当前行存在时）。
**下次注意**：不要在仅收到ANSI序列时创建空行，避免污染smart/full输出。
- [x] 7. 扩展 Claude Code 交互特征检测（Schlepping/Calculating 等）
**关键做法**：在 src/terminal-manager.ts: isLikelyChatInteractiveTail 增加 Claude Code 运行状态词（Schlepping/Calculating）用于更稳定地识别交互式会话。
**下次注意**：无
- [x] 8. interact_with_terminal：当 input+appendNewline 且检测交互式时自动改用 keySequence（text+enter）
**关键做法**：在 src/mcp-server.ts: 当检测为交互式且使用 input+appendNewline 时自动转换为 keySequence(text+enter)，并在 structuredContent 标记 autoKeySequence=true。
**下次注意**：specialOperation/keys/keySequence 显式提供时必须优先，不能被自动转换覆盖。
- [x] 9. 按规定脚本构建后端并跑自测，验证 smart/full 输出可读性与交互式回车
**关键做法**：按规定脚本重新 build + 启动 + reload + 重启 MCP + 跑 src/tests/test-mcp-client.mjs；另用 node 冒烟验证 autoKeySequence=true 可触发并执行成功。
**下次注意**：无
- [x] 10. 修复 claude TUI 输出不可见：smart/full 从 rawOutput 渲染屏幕快照
**关键做法**：在 `src/terminal-manager.ts` 的 `readFromTerminal` 增加交互式TUI分支：smart/auto/full 下基于 rawOutput 进行 ANSI 屏幕快照渲染（支持常见CSI光标移动/清屏/清行/定位等），避免 OutputBuffer 输出重复或丢失。
**下次注意**：避免仅在 alternateScreen 才回退，否则 claude 这种非备用屏重绘仍会不可读。
- [x] 11. 为 TerminalSession 记录 cols/rows 以提升屏幕渲染准确性
**关键做法**：在 `src/types.ts` 给 `TerminalSession` 新增可选 `cols/rows`，并在 `src/terminal-manager.ts` 的 `createTerminal` 写入，用于屏幕快照渲染。
**下次注意**：保持字段可选，避免破坏兼容性。
- [x] 12. 用CTI实际启动claude验证可读取到界面内容
**关键做法**：使用 mcphub 的 `CTI_interact_with_terminal` 启动 `claude`，再发送 `enter` 触发读取，确认 `smart` 模式返回稳定“屏幕快照”可看到界面文本。
**下次注意**：无
- [x] 13. 修正ANSI屏幕渲染ED/EL语义，确保Claude Code回复在smart可见
**关键做法**：在 `src/terminal-manager.ts` 的 `renderAnsiScreenSnapshot` 修正 ANSI ED/EL（`CSI J/K`）清除语义：区分 0/1/2（从光标到末尾/从开头到光标/整屏或整行），避免 TUI 重绘时把回复文本误清空。
**下次注意**：不要把 `J 0`/`K 0` 简化成整屏/整行清空，否则会导致 smart 屏幕快照只剩空白或分隔线。
- [x] 14. 在 interact_with_terminal 增加正则搜索终端内容参数与实现（不新增工具）
**关键做法**：在原有 `interact_with_terminal` 工具上新增搜索参数 `search/searchRegex/caseSensitive/contextLines/maxMatches/searchSince`，服务端对 `OutputBuffer` 做正则/文本匹配并返回命中行+上下文到 `structuredContent.search`，同时把结果摘要追加到文本响应。
**下次注意**：搜索基于缓冲区行内容（非 rawOutput 屏幕快照）；需要更贴近TUI屏幕时可后续扩展 searchIn=raw/screen 但不应破坏默认行为。
