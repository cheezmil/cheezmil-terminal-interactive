- [x] 1. 定位 interact_with_terminal 中交互状态拦截导致无法继续输入的代码路径
**关键解决方法**：在 `src/mcp-server.ts` 将交互状态检测从“提前 return 阻断输入”改为“仅写入 warnings/structuredContent 并继续写入终端与读取输出”。新增 `structuredContent.warnings`，并在最终 `responseText` 前缀附加提示文本，确保提示只是附带信息且实际操作结果仍返回。更新 `src/tests/test-mcp-client.mjs` 回归测试：触发 running 状态后再次发送输入，验证非 error、文本包含提示、`structuredContent.warnings` 含提示且 `structuredContent.input` 证明输入已被处理。已按既定脚本完成后端构建/重启并跑测试通过。
- [x] 2. 将交互状态分支改为“仅提示+附带状态”，不再提前 return，继续写入并返回读取结果
**关键解决方法**：在 `src/mcp-server.ts` 将交互状态检测从“提前 return 阻断输入”改为“仅写入 warnings/structuredContent 并继续写入终端与读取输出”。新增 `structuredContent.warnings`，并在最终 `responseText` 前缀附加提示文本，确保提示只是附带信息且实际操作结果仍返回。更新 `src/tests/test-mcp-client.mjs` 回归测试：触发 running 状态后再次发送输入，验证非 error、文本包含提示、`structuredContent.warnings` 含提示且 `structuredContent.input` 证明输入已被处理。已按既定脚本完成后端构建/重启并跑测试通过。
- [x] 3. 保证对Claude Code（备用屏幕/等待输入）场景可正常发送输入，必要时补充参数或识别逻辑
**关键解决方法**：在 `src/mcp-server.ts` 将交互状态检测从“提前 return 阻断输入”改为“仅写入 warnings/structuredContent 并继续写入终端与读取输出”。新增 `structuredContent.warnings`，并在最终 `responseText` 前缀附加提示文本，确保提示只是附带信息且实际操作结果仍返回。更新 `src/tests/test-mcp-client.mjs` 回归测试：触发 running 状态后再次发送输入，验证非 error、文本包含提示、`structuredContent.warnings` 含提示且 `structuredContent.input` 证明输入已被处理。已按既定脚本完成后端构建/重启并跑测试通过。
- [x] 4. 按既定脚本重建/重启后端并运行测试客户端验证
**关键解决方法**：在 `src/mcp-server.ts` 将交互状态检测从“提前 return 阻断输入”改为“仅写入 warnings/structuredContent 并继续写入终端与读取输出”。新增 `structuredContent.warnings`，并在最终 `responseText` 前缀附加提示文本，确保提示只是附带信息且实际操作结果仍返回。更新 `src/tests/test-mcp-client.mjs` 回归测试：触发 running 状态后再次发送输入，验证非 error、文本包含提示、`structuredContent.warnings` 含提示且 `structuredContent.input` 证明输入已被处理。已按既定脚本完成后端构建/重启并跑测试通过。
