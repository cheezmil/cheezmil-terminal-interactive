- [x] 1. 梳理现有设置存储与MCP命令执行入口
**关键解决方法**：确认配置由 src/config-manager.ts + /api/settings 读写；前端 SettingsView.vue 负责编辑 mcp.disabledTools 等；MCP 端命令输入从 src/mcp-server.ts 的 interact_with_terminal 工具写入终端。
- [x] 2. 定义命令黑名单配置结构（命令+自定义提示+大小写开关）并接入持久化
**关键解决方法**：在默认配置中新增 mcp.commandBlacklist: { caseInsensitive, rules[{command,message?}] }；前端 SettingsView 默认模板与 pinia 类型同步补齐，保证 /api/settings 读写时结构不缺字段。
- [x] 3. 在MCP执行前做黑名单匹配，命中则严格禁止执行并返回提示消息
**关键解决方法**：在 src/mcp-server.ts 的 interact_with_terminal 写入前增加 checkCommandBlacklist：按配置 rules 匹配命令（支持忽略大小写），命中则返回自定义/默认提示并 isError=true，严格不调用 writeToTerminal。
- [x] 4. 前端设置页新增黑名单管理UI与“不区分命令大小写”开关，接入i18n与pinia
**关键解决方法**：SettingsView.vue MCP 配置区新增命令黑名单 UI：caseInsensitive 开关 + rules 列表（command/message）；并在 zh/en i18n 增加相关文案。
- [x] 5. 按项目脚本构建/启动前后端并用chrome-mcp检查console与页面样式
**关键解决方法**：已按脚本完成前后端 build+start；chrome-mcp 访问 http://localhost:1107/settings 控制台 0 报错并确认页面渲染；同时运行 src/tests/test-mcp-client.mjs 验证黑名单命中时返回自定义“XXXXX”且 isError=true。
