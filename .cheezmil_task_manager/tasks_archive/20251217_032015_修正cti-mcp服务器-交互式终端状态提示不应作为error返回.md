- [x] 1. 梳理当前MCP里“交互状态”返回路径与调用方
**关键解决方法**：在 `src/mcp-server.ts` 将“交互状态”分支从 `isError: true` 改为普通提示（不设置 `isError`），并把文案更新为“该终端进入了交互式终端，请根据终端内容做出合理行动”。同时在 `src/tests/test-mcp-client.mjs` 增加回归测试：通过 `Start-Sleep -Seconds 5` 触发终端 running 状态，再发新命令验证返回为非 error 且提示文案正确。
- [x] 2. 把“Error: 该终端进入了交互状态...”改为“提示：该终端进入了交互式终端，请根据终端内容做出合理行动”且不要走error分支
**关键解决方法**：在 `src/mcp-server.ts` 将“交互状态”分支从 `isError: true` 改为普通提示（不设置 `isError`），并把文案更新为“该终端进入了交互式终端，请根据终端内容做出合理行动”。同时在 `src/tests/test-mcp-client.mjs` 增加回归测试：通过 `Start-Sleep -Seconds 5` 触发终端 running 状态，再发新命令验证返回为非 error 且提示文案正确。
- [x] 3. 确保客户端/测试用例能区分error与notice并验证输出
**关键解决方法**：在 `src/mcp-server.ts` 将“交互状态”分支从 `isError: true` 改为普通提示（不设置 `isError`），并把文案更新为“该终端进入了交互式终端，请根据终端内容做出合理行动”。同时在 `src/tests/test-mcp-client.mjs` 增加回归测试：通过 `Start-Sleep -Seconds 5` 触发终端 running 状态，再发新命令验证返回为非 error 且提示文案正确。
