- [x] 1. 调整 App.vue 顶部栏布局：版本号靠左
**关键解决方法**：把版本号展示从顶部栏右侧区域挪到了左侧 Logo 图标右边，并用 chrome-mcp 验证 header DOM 中 Logo 后紧跟 v0.8.0。
**不能犯的错误**：直接运行 `ct` 在 PowerShell 中可能找不到（当前目录不在 PATH）。本仓库已有 `ct.cmd` 兼容启动器，可用 `.\ct.cmd start -- ...` 启动；若需严格 `ct` 无前缀可用，需要把项目根目录加入 PATH。
- [x] 2. 按前端脚本构建并启动产物
**关键解决方法**：把版本号展示从顶部栏右侧区域挪到了左侧 Logo 图标右边，并用 chrome-mcp 验证 header DOM 中 Logo 后紧跟 v0.8.0。
**不能犯的错误**：直接运行 `ct` 在 PowerShell 中可能找不到（当前目录不在 PATH）。本仓库已有 `ct.cmd` 兼容启动器，可用 `.\ct.cmd start -- ...` 启动；若需严格 `ct` 无前缀可用，需要把项目根目录加入 PATH。
- [x] 3. Chrome 刷新验证 DOM/控制台
**关键解决方法**：把版本号展示从顶部栏右侧区域挪到了左侧 Logo 图标右边，并用 chrome-mcp 验证 header DOM 中 Logo 后紧跟 v0.8.0。
**不能犯的错误**：直接运行 `ct` 在 PowerShell 中可能找不到（当前目录不在 PATH）。本仓库已有 `ct.cmd` 兼容启动器，可用 `.\ct.cmd start -- ...` 启动；若需严格 `ct` 无前缀可用，需要把项目根目录加入 PATH。
