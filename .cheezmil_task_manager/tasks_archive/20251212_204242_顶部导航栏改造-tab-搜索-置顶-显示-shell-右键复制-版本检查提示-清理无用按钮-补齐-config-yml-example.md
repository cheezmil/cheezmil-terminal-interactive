- [x] 1. 实现后端版本检查与 /api/version
**关键解决方法**：实现了后端版本服务（读取本地 VERSION + 启动时拉取 GitHub VERSION 对比）并暴露 /api/version；前端顶部栏新增搜索框、终结全部按钮、版本/更新提示并移除无用统计；Tab 列表支持搜索过滤、置顶、显示 shell、移除重连/清空、去掉 active 文案；xterm 选中文本右键复制；移除主内容区多余 padding；补齐 config.yml.example 新增配置项；按脚本构建/启动并用 chrome-mcp 验证无报错。
**不能犯的错误**：后端新增路由需放在 setupFrontendApiRoutes（有 terminalManager 作用域）中，误放到 settings 路由会导致 tsc 找不到 terminalManager。
- [x] 2. 前端顶部栏：终结全部/搜索框/版本提示/移除统计
**关键解决方法**：实现了后端版本服务（读取本地 VERSION + 启动时拉取 GitHub VERSION 对比）并暴露 /api/version；前端顶部栏新增搜索框、终结全部按钮、版本/更新提示并移除无用统计；Tab 列表支持搜索过滤、置顶、显示 shell、移除重连/清空、去掉 active 文案；xterm 选中文本右键复制；移除主内容区多余 padding；补齐 config.yml.example 新增配置项；按脚本构建/启动并用 chrome-mcp 验证无报错。
**不能犯的错误**：后端新增路由需放在 setupFrontendApiRoutes（有 terminalManager 作用域）中，误放到 settings 路由会导致 tsc 找不到 terminalManager。
- [x] 3. 前端 Tab 列表：搜索过滤/置顶/显示 shell/移除重连清空/去掉 active 文案
**关键解决方法**：实现了后端版本服务（读取本地 VERSION + 启动时拉取 GitHub VERSION 对比）并暴露 /api/version；前端顶部栏新增搜索框、终结全部按钮、版本/更新提示并移除无用统计；Tab 列表支持搜索过滤、置顶、显示 shell、移除重连/清空、去掉 active 文案；xterm 选中文本右键复制；移除主内容区多余 padding；补齐 config.yml.example 新增配置项；按脚本构建/启动并用 chrome-mcp 验证无报错。
**不能犯的错误**：后端新增路由需放在 setupFrontendApiRoutes（有 terminalManager 作用域）中，误放到 settings 路由会导致 tsc 找不到 terminalManager。
- [x] 4. xterm 选中文本右键复制
**关键解决方法**：实现了后端版本服务（读取本地 VERSION + 启动时拉取 GitHub VERSION 对比）并暴露 /api/version；前端顶部栏新增搜索框、终结全部按钮、版本/更新提示并移除无用统计；Tab 列表支持搜索过滤、置顶、显示 shell、移除重连/清空、去掉 active 文案；xterm 选中文本右键复制；移除主内容区多余 padding；补齐 config.yml.example 新增配置项；按脚本构建/启动并用 chrome-mcp 验证无报错。
**不能犯的错误**：后端新增路由需放在 setupFrontendApiRoutes（有 terminalManager 作用域）中，误放到 settings 路由会导致 tsc 找不到 terminalManager。
- [x] 5. 去除主区域多余 padding
**关键解决方法**：实现了后端版本服务（读取本地 VERSION + 启动时拉取 GitHub VERSION 对比）并暴露 /api/version；前端顶部栏新增搜索框、终结全部按钮、版本/更新提示并移除无用统计；Tab 列表支持搜索过滤、置顶、显示 shell、移除重连/清空、去掉 active 文案；xterm 选中文本右键复制；移除主内容区多余 padding；补齐 config.yml.example 新增配置项；按脚本构建/启动并用 chrome-mcp 验证无报错。
**不能犯的错误**：后端新增路由需放在 setupFrontendApiRoutes（有 terminalManager 作用域）中，误放到 settings 路由会导致 tsc 找不到 terminalManager。
- [x] 6. 同步更新 config.yml.example
**关键解决方法**：实现了后端版本服务（读取本地 VERSION + 启动时拉取 GitHub VERSION 对比）并暴露 /api/version；前端顶部栏新增搜索框、终结全部按钮、版本/更新提示并移除无用统计；Tab 列表支持搜索过滤、置顶、显示 shell、移除重连/清空、去掉 active 文案；xterm 选中文本右键复制；移除主内容区多余 padding；补齐 config.yml.example 新增配置项；按脚本构建/启动并用 chrome-mcp 验证无报错。
**不能犯的错误**：后端新增路由需放在 setupFrontendApiRoutes（有 terminalManager 作用域）中，误放到 settings 路由会导致 tsc 找不到 terminalManager。
- [x] 7. 按脚本构建并用 chrome-mcp 验证
**关键解决方法**：实现了后端版本服务（读取本地 VERSION + 启动时拉取 GitHub VERSION 对比）并暴露 /api/version；前端顶部栏新增搜索框、终结全部按钮、版本/更新提示并移除无用统计；Tab 列表支持搜索过滤、置顶、显示 shell、移除重连/清空、去掉 active 文案；xterm 选中文本右键复制；移除主内容区多余 padding；补齐 config.yml.example 新增配置项；按脚本构建/启动并用 chrome-mcp 验证无报错。
**不能犯的错误**：后端新增路由需放在 setupFrontendApiRoutes（有 terminalManager 作用域）中，误放到 settings 路由会导致 tsc 找不到 terminalManager。
