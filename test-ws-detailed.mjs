import WebSocket from 'ws'; console.log('Creating WebSocket connection...'); const ws = new WebSocket('ws://localhost:1106'); ws.on('open', () => { console.log('WebSocket connected successfully!'); setTimeout(() => ws.close(), 1000); }); ws.on('error', (err) => { console.error('WebSocket error:', err); }); ws.on('close', (code, reason) => { console.log('WebSocket closed:', code, reason); }); setTimeout(() => { if (ws.readyState === WebSocket.CONNECTING) { console.log('Connection timeout'); ws.terminate(); } }, 5000);
